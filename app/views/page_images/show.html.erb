<script src="http://open.web.meitu.com/sources/xiuxiu.js" type="text/javascript"></script>
<script type="text/javascript">
window.onload=function(){
  xiuxiu.embedSWF("altContent",1,"100%","100%");
  /*第1个参数是加载编辑器div容器，第2个参数是编辑器类型，第3个参数是div容器宽，第4个参数是div容器高*/
  xiuxiu.setUploadURL("http://www.yufuwu.cn/page_images");//修改为上传接收图片程序地址
  xiuxiu.onInit = function ()
  {
    xiuxiu.loadPhoto("<%= @user.image.url %>");//修改为要处理的图片url
  }
  xiuxiu.onUploadResponse = function (data)
  {
    //alert("上传响应" + data); 可以开启调试
  }
}
</script>

<p id="notice"><%= notice %></p>

<p>
  <strong>Page:</strong>
  <%= @page_image.page %>
</p>

<p>
  <strong>Image:</strong>
  <%= @page_image.image %>
</p>

<%= image_tag @user.image.url %>
<%= image_tag @user.image.url(:original) %>
<%#= image_tag @user.avatar.url(:thumb) %>


<%= link_to 'Edit', edit_page_image_path(@page_image) %> |
<%= link_to 'Back', page_images_path %>
