<%= simple_form_for([current_user, @payment]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :price %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, '充值' %>
  </div>
<% end %>
